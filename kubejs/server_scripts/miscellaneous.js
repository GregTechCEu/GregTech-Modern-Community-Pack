ServerEvents.recipes(event => {
})
  /*
  // WAP
  event.remove({id: })
  event.shaped(, [
    '',
    '',
    ''
  ] , {
  })

  event.remove({id: })
  event.shapeless(, [
  ])

  event.remove({id: ""})
  event.recipes.gtceu.alloy_smelter('gtceu:')
    .itemInputs()
    .itemOutputs()
    .duration()
    .EUt()
*/